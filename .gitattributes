# 面向 Linux 部署：仓库内与检出后都统一 LF，避免 Windows 上编辑后 shell 脚本带 CR 跑不起来
* text=auto eol=lf

# 二进制（不做行尾转换）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
